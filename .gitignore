# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
.next/
out/
dist/
build/

# Expo / React Native
.expo/
*.jsbundle
*.hbc
ios/
android/
*.orig.*

# EAS Build
.eas/

# Environment files — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
apps/api/.env.local
apps/mobile/.env

# Package manager
.npm
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
package-lock.json
yarn.lock
pnpm-lock.yaml
!apps/mobile/package-lock.json
# Commit the root lockfile: Vercel's monorepo install runs a frozen install at
# the repo root and fails (EUSAGE) when no root package-lock.json is checked in.
!/package-lock.json

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V4
.Trashes
ehthumbs.db
Thumbs.db

# IDEs
.idea/
.vscode/settings.json
*.swp
*.swo

# TypeScript
*.tsbuildinfo
tsconfig.json.*

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel
.env*

# Transient / local-only files (keep the working tree clean so EAS doesn't
# prompt about uncommitted changes mid-build)
my-app/
Eat_Out_Better_GTM_Launch_Tracker.xlsx
HANDOFF-build6-ui.md

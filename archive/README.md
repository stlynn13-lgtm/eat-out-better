# Archive — superseded docs

These files are kept for history only. They are **not** maintained and **not** a source of truth. Nothing in the app, build, or config reads them (verified 2026-06-22).

Replaced by the current system (see `../CLAUDE.md` → Documentation System):
- `CHANGELOG.md` → now **`../log.md`** (plain-language "what changed").
- `V0-launch-checklist.md` → now **`../plan.md`** + the **GTM Launch Tracker** (.xlsx).
- `session-01..05` → now **`../log.md`** (one entry per working session).

Safe to read for context; do not edit. If something here is still true and live, move it into `log.md`, `plan.md`, or `backlog.md`, not back here.
